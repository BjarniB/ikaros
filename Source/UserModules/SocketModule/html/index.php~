<?php
$array = $_POST['servo'];

$send = "";

for ($i = 0; $i < count($array); $i++) {

	$send[$i] .= $i;
	for ($n = 0; $n < count($array[$i]); $n++) {
		$send[$i] .= " ".round($array[$i][$n][1],2).":".round($array[$i][$n][2],2); 
   	
    }
}

echo $send[0];



?>
